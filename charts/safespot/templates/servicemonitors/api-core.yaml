apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: api-core
  namespace: {{ .Values.namespaces.monitoring }}
  labels:
    {{- include "safespot-ops.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      app: {{ .Values.services.apiCore.appLabel }}
  endpoints:
    - port: {{ .Values.services.apiCore.metricsPortName }}
      path: {{ .Values.services.apiCore.metricsPath }}
      interval: {{ .Values.global.scrapeInterval }}
  namespaceSelector:
    matchNames:
      - {{ .Values.namespaces.application }}