apiVersion: v2
name: safespot-observability
description: SafeSpot Observability Stack — Prometheus + CloudWatch + Grafana
type: application
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  - name: kube-prometheus-stack
    version: "58.x.x"
    repository: https://prometheus-community.github.io/helm-charts
    condition: kube-prometheus-stack.enabled

maintainers:
  - name: safespot-platform