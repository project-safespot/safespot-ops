apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: redis-exporter
  namespace: {{ .Values.namespaces.monitoring }}
  labels:
    {{- include "safespot-ops.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      app: {{ .Values.services.redisExporter.appLabel }}
  endpoints:
    - port: {{ .Values.services.redisExporter.metricsPortName }}
      path: {{ .Values.services.redisExporter.metricsPath }}
      interval: {{ .Values.global.scrapeInterval }}
  namespaceSelector:
    matchNames:
      - {{ .Values.namespaces.cache }}